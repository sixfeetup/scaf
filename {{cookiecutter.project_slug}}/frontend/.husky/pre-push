cd frontend
if test -f ./.env; then
  source ./.env
  if [ "$IS_PRE_PUSH_HOOKS_ENABLED" = "true" ] || [ "$IS_PRE_PUSH_HOOKS_ENABLED" = "1" ]; then
    npm run format-check
    npm run lint-src
    npm run typecheck
  fi
fi
